#include<bits/stdc++.h>
using namespace std;
int main()
{
     //cin.ignore() it is used for to ignore new line after test cases number
    int t;
    cin>>t;
    cin.ignore();
    while(t--)
    {
        string s;
        getline(cin,s);
        cout<<s<<endl;
    }
    return 0;
}